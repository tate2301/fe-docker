import {
    string,
    number,
} from 'prop-types';

// eslint-disable-next-line import/prefer-default-export
export const RatingType = {
    label: string,
    totalStars: number,
    starsFilled: number,
};
