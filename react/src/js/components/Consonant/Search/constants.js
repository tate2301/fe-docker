export const SearchInputDefaultProps = {
    name: '',
    value: '',
    autofocus: true,
    placeholderText: '',
};

/**
 * Used as unique id for accessibility labels/attributes
 * @type {Number}
 */
export const searchInputId = 'consonant-search';
